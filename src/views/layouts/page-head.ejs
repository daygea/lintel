<div class="page-head">
  <% if (typeof eyebrow !== 'undefined' && eyebrow) { %><div class="eyebrow"><%= eyebrow %></div><% } %>
  <h1><%= heading %></h1>
  <% if (typeof lede !== 'undefined' && lede) { %><p class="lede"><%= lede %></p><% } %>
</div>
